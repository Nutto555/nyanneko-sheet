---
phase: 06-frontend-ui-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/pages/Home.tsx
  - src/pages/GvgMode.tsx
  - src/pages/About.tsx
  - src/pages/Pets.tsx
  - src/pages/Rings.tsx
  - src/pages/EquipmentSets.tsx
  - src/pages/Equip.tsx
  - src/pages/Updates.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "All member-facing pages use standardized py-8 padding"
    - "All pages with headers use PageHeader component or display font heading"
    - "Data-heavy pages (Pets, Rings, EquipmentSets) use max-w-7xl"
    - "Strategy pages (Home, GvgMode) use max-w-5xl"
    - "About page uses max-w-3xl"
    - "Loading states use Skeleton components with dark theme shimmer"
    - "All pages have page-enter transition class"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Home page with max-w-5xl and py-8"
      contains: "py-8"
    - path: "src/pages/GvgMode.tsx"
      provides: "GvG mode with max-w-5xl, PageHeader, and py-8"
      contains: "PageHeader"
    - path: "src/pages/About.tsx"
      provides: "About page with max-w-3xl, PageHeader, and py-8"
      contains: "PageHeader"
    - path: "src/pages/Pets.tsx"
      provides: "Pets page with max-w-7xl, PageHeader, and py-8"
      contains: "PageHeader"
  key_links:
    - from: "src/pages/GvgMode.tsx"
      to: "src/components/ui/PageHeader.tsx"
      via: "import"
      pattern: "import.*PageHeader"
    - from: "src/pages/Pets.tsx"
      to: "src/components/ui/Skeleton.tsx"
      via: "import"
      pattern: "import.*Skeleton"
---

<objective>
Standardize all remaining member-facing pages with consistent container widths, PageHeader, py-8 padding, page-enter transitions, and loading skeletons.

Purpose: Per D-05 Wave 3, polish GvgMode and remaining member pages (Home, Pets, Rings, Equipment, Equip, About, Updates). Per D-03, add loading skeletons that match real content layout.
Output: All 8 member pages updated with consistent layout patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-ui-polish/06-CONTEXT.md
@.planning/phases/06-frontend-ui-polish/06-UI-SPEC.md
@.planning/phases/06-frontend-ui-polish/06-01-SUMMARY.md

<interfaces>
<!-- PageHeader from Plan 01 -->
```typescript
export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  className?: string;
}
export function PageHeader(props: PageHeaderProps): React.JSX.Element;
```

<!-- Skeleton primitives from Plan 01 -->
```typescript
export function Skeleton({ className, style }: { className?: string; style?: React.CSSProperties }): React.JSX.Element;
export function SkeletonCard({ className }: { className?: string }): React.JSX.Element;
export function SkeletonText({ lines, className }: { lines?: number; className?: string }): React.JSX.Element;
```

<!-- Page container width contract from UI-SPEC -->
| Page Category | max-w Class | Pages |
|---------------|-------------|-------|
| Data-heavy (grids of cards) | max-w-7xl | Characters, Pets, Rings, Equipment |
| Strategy/form pages | max-w-5xl | CounterSearch, AdminStrategies, GvgMode, Home |
| Minimal content | max-w-3xl | About |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Home, GvgMode, About, and Updates pages</name>
  <files>src/pages/Home.tsx, src/pages/GvgMode.tsx, src/pages/About.tsx, src/pages/Updates.tsx</files>
  <read_first>
    - src/pages/Home.tsx (current: max-w-5xl py-10)
    - src/pages/GvgMode.tsx (current: max-w-4xl py-8, has inline page header)
    - src/pages/About.tsx (current: max-w-3xl py-16)
    - src/pages/Updates.tsx (current layout)
    - src/components/ui/PageHeader.tsx (component from plan 01)
    - .planning/phases/06-frontend-ui-polish/06-UI-SPEC.md (container widths and copy)
  </read_first>
  <action>
**Home.tsx changes:**
1. Change `py-10` to `py-8` in container div (standardized padding)
2. Add `page-enter` class to container div: `className="page-enter max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8"`
3. max-w-5xl is already correct — no change needed
4. Home page has a unique hero section — do NOT replace with PageHeader (hero is the display-role title, not a standard page header)

**GvgMode.tsx changes:**
1. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
2. Change `max-w-4xl` to `max-w-5xl` in the main container (strategy page)
3. Replace the inline page header block (lines 167-184, the `<div className="mb-8">` with h1, title_th, description, gold divider) with:
   ```tsx
   <div className="flex items-center gap-3 mb-2">
     <span className="text-3xl">{config.icon}</span>
     <PageHeader title={config.title_en} subtitle={config.description} />
   </div>
   ```
   Wait — GvgMode's header includes an icon and Thai subtitle which PageHeader doesn't support. Better approach: Keep the icon rendering, use PageHeader for the title+description, skip the Thai subtitle (it's redundant with the title). Updated approach:
   ```tsx
   <div className="flex items-center gap-3">
     <span className="text-3xl">{config.icon}</span>
     <div className="flex-1">
       <PageHeader title={config.title_en} subtitle={config.description} />
     </div>
   </div>
   ```
   This wraps PageHeader in a flex container with the icon. The Thai subtitle (`config.title_th`) can be added as a small text below the title but before the divider. Since PageHeader doesn't support this, add it between the icon and PageHeader as a separate element OR just drop it since it's already shown in the nav label. Decision: Keep a small Thai label under the English title for GvG users. Simplest approach — add it right after the icon:
   ```tsx
   <div className="mb-8">
     <div className="flex items-center gap-3 mb-2">
       <span className="text-3xl">{config.icon}</span>
       <div>
         <h1 className="text-2xl font-bold text-white" style={{ fontFamily: 'var(--font-display)' }}>{config.title_en}</h1>
         <p className="text-sm opacity-50" style={{ fontFamily: 'var(--font-body)' }}>{config.title_th}</p>
       </div>
     </div>
     <p className="text-sm text-slate-400 mt-2 max-w-xl">{config.description}</p>
     <div className="mt-3 h-px" style={{ background: 'linear-gradient(to right, var(--color-gold-dim), transparent)' }} />
   </div>
   ```
   Actually this is ALREADY exactly what GvgMode has. The only changes needed are: `max-w-4xl` to `max-w-5xl` and adding `page-enter` class. The header pattern is already correct with font-display. So: do NOT import PageHeader for GvgMode — its custom header with icon + Thai text already matches the design system.
4. Final GvgMode changes: change `max-w-4xl` to `max-w-5xl` and add `page-enter` class to container. Change the `max-w-4xl` in the "mode not found" fallback to `max-w-5xl` too.
5. Add `page-enter` class to container: `className="page-enter max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8"`

**About.tsx changes:**
1. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
2. Change `py-16` to `py-8` in container
3. Replace `<h1 className="text-4xl font-bold text-white mb-8">About NyanNeko Sheet</h1>` with: `<PageHeader title="About NyanNeko Sheet" />`
4. Add `page-enter` class to container div

**Updates.tsx changes:**
1. Read the file to find current container and header pattern
2. Standardize container padding to `py-8` if different
3. Add PageHeader if page has a manual title element (use `title="Game Updates"` and `subtitle="Latest patch notes and meta changes"`)
4. Add `page-enter` class to container div
5. Ensure responsive grid works at mobile widths
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Home.tsx contains `py-8` (NOT py-10) and `page-enter` class
    - Home.tsx still contains `max-w-5xl` (unchanged)
    - GvgMode.tsx contains `max-w-5xl` (NOT max-w-4xl)
    - GvgMode.tsx contains `page-enter` class
    - GvgMode.tsx still contains `fontFamily: 'var(--font-display)'` (existing header preserved)
    - About.tsx contains `import { PageHeader }` and `<PageHeader`
    - About.tsx contains `py-8` (NOT py-16) and `page-enter` class
    - About.tsx contains `title="About NyanNeko Sheet"`
    - Updates.tsx contains `py-8` and `page-enter` class
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Home, GvgMode, About, and Updates pages all use standardized padding, correct max-w, and page-enter transition.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Pets, Rings, EquipmentSets, and Equip pages with PageHeader and loading skeletons</name>
  <files>src/pages/Pets.tsx, src/pages/Rings.tsx, src/pages/EquipmentSets.tsx, src/pages/Equip.tsx</files>
  <read_first>
    - src/pages/Pets.tsx (current layout — max-w-7xl py-16)
    - src/pages/Rings.tsx (current layout — max-w-7xl py-16)
    - src/pages/EquipmentSets.tsx (current layout)
    - src/pages/Equip.tsx (current layout, has its own Skeleton function)
    - src/components/ui/PageHeader.tsx (component from plan 01)
    - src/components/ui/Skeleton.tsx (skeleton primitives from plan 01)
    - .planning/phases/06-frontend-ui-polish/06-UI-SPEC.md (container widths)
  </read_first>
  <action>
Apply the same standardization pattern to all 4 data-heavy/reference pages:

**For each page (Pets, Rings, EquipmentSets):**
1. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
2. Import Skeleton primitives: `import { SkeletonCard } from '../components/ui/Skeleton';`
3. Change `py-16` to `py-8` in all container divs (main, loading, error states)
4. Replace inline `<h1>` + subtitle text with `<PageHeader>`:
   - Pets: `<PageHeader title="Pet Database" subtitle="Browse all pets and their abilities" />`
   - Rings: `<PageHeader title="Ring Database" subtitle="Browse all rings and their effects" />`
   - EquipmentSets: `<PageHeader title="Equipment Sets" subtitle="Browse equipment set bonuses and effects" />`
5. Replace loading state `<p className="text-gray-400">Loading...</p>` with a grid of SkeletonCards:
   ```tsx
   <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
     <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
       {Array.from({ length: 8 }, (_, i) => <SkeletonCard key={i} />)}
     </div>
   </div>
   ```
6. Add `page-enter` class to main container div
7. max-w-7xl is already correct for data-heavy pages — no change needed

**Equip.tsx specific changes:**
1. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
2. Read the file fully to find current header and container pattern
3. Replace inline header with: `<PageHeader title="Equipment Guide" subtitle="Per-character equipment stat priorities for each skill slot" />`
4. Standardize padding to `py-8`
5. Replace the local `Skeleton` function with the shared SkeletonCard from `../components/ui/Skeleton` (remove the local function definition to avoid duplication)
6. Add `page-enter` class to container
7. Equip is a reference page — use `max-w-7xl` (same as other data-heavy pages)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Pets.tsx contains `import { PageHeader }` and `<PageHeader title="Pet Database"`
    - Pets.tsx contains `py-8` (NOT py-16)
    - Pets.tsx contains `SkeletonCard` for loading state
    - Pets.tsx contains `page-enter` class
    - Rings.tsx contains `import { PageHeader }` and `<PageHeader title="Ring Database"`
    - Rings.tsx contains `py-8` (NOT py-16)
    - Rings.tsx contains `SkeletonCard` for loading state
    - EquipmentSets.tsx contains `import { PageHeader }` and `<PageHeader`
    - EquipmentSets.tsx contains `py-8` and `page-enter` class
    - Equip.tsx contains `import { PageHeader }` and `<PageHeader title="Equipment Guide"`
    - Equip.tsx contains `py-8` and `page-enter` class
    - Equip.tsx does NOT contain a local `function Skeleton()` definition (uses shared component)
    - npm run build exits 0
  </acceptance_criteria>
  <done>All data-heavy member pages use PageHeader, SkeletonCard loading states, py-8 padding, max-w-7xl containers, and page-enter transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Purely visual changes — no data flow, auth, or input modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No security surface in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- All 8 pages use standardized padding (py-8), correct max-w, and page-enter transitions
- Pages with data loading use SkeletonCard components
</verification>

<success_criteria>
- Every member-facing page has consistent container width (max-w-7xl for data, max-w-5xl for strategy, max-w-3xl for minimal)
- Every page uses py-8 standardized vertical padding
- Every page has page-enter transition class
- Loading states use dark-themed skeleton shimmer matching content layout
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-polish/06-03-SUMMARY.md`
</output>
