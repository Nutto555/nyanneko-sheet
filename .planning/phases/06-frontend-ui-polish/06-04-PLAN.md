---
phase: 06-frontend-ui-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
files_modified:
  - src/pages/Admin.tsx
  - src/pages/AdminStrategies.tsx
  - src/pages/AdminTemplates.tsx
autonomous: false
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Admin page uses standardized py-8 padding and page-enter transition"
    - "AdminStrategies uses max-w-5xl container and PageHeader"
    - "AdminTemplates uses max-w-5xl container and PageHeader"
    - "Admin pages use loading skeletons instead of plain text loading states"
    - "All admin pages have consistent spacing with member-facing pages"
  artifacts:
    - path: "src/pages/Admin.tsx"
      provides: "Admin team editor with standardized layout"
      contains: "page-enter"
    - path: "src/pages/AdminStrategies.tsx"
      provides: "Strategy builder with PageHeader and max-w-5xl"
      contains: "PageHeader"
    - path: "src/pages/AdminTemplates.tsx"
      provides: "Template manager with PageHeader and max-w-5xl"
      contains: "PageHeader"
  key_links:
    - from: "src/pages/AdminStrategies.tsx"
      to: "src/components/ui/PageHeader.tsx"
      via: "import"
      pattern: "import.*PageHeader"
---

<objective>
Standardize all 3 admin pages with consistent layout, PageHeader, loading skeletons, and page transitions. Per D-06, admin pages get the SAME full treatment as member-facing pages.

Purpose: Per D-05 Wave 4, polish admin pages last but with full parity (D-06). Includes a final human verification checkpoint to confirm the entire phase visually.
Output: Updated Admin.tsx, AdminStrategies.tsx, AdminTemplates.tsx + human visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-ui-polish/06-CONTEXT.md
@.planning/phases/06-frontend-ui-polish/06-UI-SPEC.md
@.planning/phases/06-frontend-ui-polish/06-01-SUMMARY.md

<interfaces>
<!-- PageHeader from Plan 01 -->
```typescript
export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  className?: string;
}
export function PageHeader(props: PageHeaderProps): React.JSX.Element;
```

<!-- Skeleton primitives from Plan 01 -->
```typescript
export function Skeleton({ className, style }: { className?: string; style?: React.CSSProperties }): React.JSX.Element;
export function SkeletonCard({ className }: { className?: string }): React.JSX.Element;
export function SkeletonText({ lines, className }: { lines?: number; className?: string }): React.JSX.Element;
```

<!-- UI-SPEC copy contract for admin pages -->
| Page header: Admin Strategies | title: "Strategy Builder" / subtitle: "Create and manage counter strategies" |
| Page header: Admin Templates | title: "Template Manager" / subtitle: "Manage enemy defense templates" |

<!-- Admin page container: max-w-4xl for team editor, max-w-5xl for strategy/template admin -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Admin, AdminStrategies, and AdminTemplates pages</name>
  <files>src/pages/Admin.tsx, src/pages/AdminStrategies.tsx, src/pages/AdminTemplates.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (current layout — full file, find container classes and header)
    - src/pages/AdminStrategies.tsx (current layout — full file, find container classes and header)
    - src/pages/AdminTemplates.tsx (current layout — full file, find container classes and header)
    - src/components/ui/PageHeader.tsx (component from plan 01)
    - src/components/ui/Skeleton.tsx (skeleton primitives from plan 01)
    - .planning/phases/06-frontend-ui-polish/06-UI-SPEC.md (copy contract, container widths)
    - .planning/phases/06-frontend-ui-polish/06-CONTEXT.md (D-06: admin gets same treatment)
  </read_first>
  <action>
**Admin.tsx changes:**
1. Read the full file to identify all container divs, headers, and loading states
2. Standardize container padding to `py-8` if different
3. Keep `max-w-4xl` (Admin is the original team editor — per UI-SPEC it stays at max-w-4xl)
4. Add `page-enter` class to main container div
5. If there is a plain text loading state (e.g., `<p>Loading...</p>`), replace with SkeletonCard grid
6. If there is an inline `<h1>` title, replace with: `import { PageHeader } from '../components/ui/PageHeader';` and `<PageHeader title="Team Manager" subtitle="Manage GvG team compositions" />`
7. Add standard horizontal padding if missing: `px-4 sm:px-6 lg:px-8`

**AdminStrategies.tsx changes:**
1. Read the full file to identify the StrategyBuilderContent component's layout
2. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
3. Find and replace inline header with: `<PageHeader title="Strategy Builder" subtitle="Create and manage counter strategies" />`
4. Change container to `max-w-5xl` (strategy/form page)
5. Standardize padding to `py-8`
6. Add `page-enter` class to outermost content wrapper inside the auth gate
7. Replace any plain text loading indicators with SkeletonCard or SkeletonText from `../components/ui/Skeleton`

**AdminTemplates.tsx changes:**
1. Read the full file to identify the TemplateManagerContent component's layout
2. Import PageHeader: `import { PageHeader } from '../components/ui/PageHeader';`
3. Find and replace inline header with: `<PageHeader title="Template Manager" subtitle="Manage enemy defense templates" />`
4. Change container to `max-w-5xl` (strategy/form page)
5. Standardize padding to `py-8`
6. Add `page-enter` class to outermost content wrapper inside the auth gate
7. Replace any plain text loading indicators with SkeletonCard or SkeletonText

**Important for all 3 admin pages:**
- These pages wrap content in `<AdminAuthGate>` — the page-enter class and PageHeader go INSIDE the auth gate (on the content wrapper), NOT on AdminAuthGate itself
- Preserve all existing admin functionality (auth gate, CRUD operations, form state)
- Only modify layout/container/header/loading elements
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin.tsx contains `py-8` and `page-enter` class
    - Admin.tsx contains `max-w-4xl` (team editor keeps 4xl)
    - AdminStrategies.tsx contains `import { PageHeader }` and `<PageHeader title="Strategy Builder"`
    - AdminStrategies.tsx contains `subtitle="Create and manage counter strategies"`
    - AdminStrategies.tsx contains `max-w-5xl`
    - AdminStrategies.tsx contains `py-8` and `page-enter` class
    - AdminTemplates.tsx contains `import { PageHeader }` and `<PageHeader title="Template Manager"`
    - AdminTemplates.tsx contains `subtitle="Manage enemy defense templates"`
    - AdminTemplates.tsx contains `max-w-5xl`
    - AdminTemplates.tsx contains `py-8` and `page-enter` class
    - npm run build exits 0
  </acceptance_criteria>
  <done>All 3 admin pages have consistent layout with member-facing pages: PageHeader, standardized padding, correct max-w, page transitions, and skeleton loading states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UI polish</name>
  <files></files>
  <action>Human verifies visual consistency across all pages by following the verification steps below. No code changes in this task.</action>
  <what-built>Complete frontend UI polish across all pages: navigation updates (Counter + Admin links), CharacterPortrait adoption, PageHeader consistency, loading skeletons, page transitions, and standardized spacing/typography/colors.</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the dev server
    2. **Navigation check (UI-04):**
       - Desktop: Verify nav shows Home | Counter | Attack | Defense (dropdown) | Database (dropdown) | Gear Guide | Admin
       - Click Counter — should navigate to /counter
       - Click Admin — should navigate to /admin
       - Resize to mobile width (< 768px) — tap hamburger menu — verify Counter and Admin appear
    3. **Portrait consistency (UI-02):**
       - Navigate to /characters — character cards show portraits via CharacterPortrait (rounded, themed border)
       - Click a character — detail page shows portrait with display font name
       - Navigate to /counter — select enemy characters — counter results show portraits in team displays
       - Navigate to GvG Attack (/attack) — team cards show portraits via UnitSlot (same rounded style as CharacterPortrait)
    4. **Layout consistency (UI-01):**
       - Visit each page and verify consistent py-8 top spacing (not too tight, not too loose)
       - Verify page headers have gold gradient divider
       - Verify card hover effects (subtle border glow on hover)
       - Verify loading states show skeleton shimmer (refresh page or throttle network)
    5. **Mobile responsiveness (UI-03):**
       - Resize browser to 375px width
       - Navigate /counter — no horizontal scroll, strategy cards readable
       - Navigate /characters — grid adjusts to 2 columns
       - Navigate /admin — forms are usable
    6. **Page transitions:**
       - Click between pages — verify subtle fade-in animation (150ms)
  </how-to-verify>
  <verify>Human visual inspection following the steps above</verify>
  <done>Human confirms all UI polish changes look correct across desktop and mobile viewports.</done>
  <resume-signal>Type "approved" or describe any issues with specific pages/components</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Purely visual changes on admin pages — auth gate unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No security surface — admin auth gate is not modified |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- All 3 admin pages use PageHeader, standardized padding, and page-enter transitions
- Human verification confirms visual consistency across all pages
</verification>

<success_criteria>
- Admin pages have the same visual quality as member-facing pages (per D-06)
- Human confirms: navigation works, portraits are consistent, layout is uniform, mobile is usable
- Full build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-polish/06-04-SUMMARY.md`
</output>
